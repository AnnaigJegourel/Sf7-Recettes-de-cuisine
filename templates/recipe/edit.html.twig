{% extends "base.html.twig" %}

{% block title "Recette : " ~ recipe.title %}

{% block body %}

    <h1>{{ recipe.title }}</h1>

    {# la fonction qui permet de passer un formulair + la variable envoyée par le contrôleur #}
    {{ form(form) }}

{% endblock %}