{% extends "base.html.twig" %}

{# syntaxe complète
{% block title %}Toutes les recettes{% endblock %}
syntaxe raccourcie#}
{% block title "Toutes les recettes !"%}

{% block body %}
    Voici les recettes disponibles en base de données :
    <ul>
        {% for recipe in recipes %}
        <li>
            {#  en appelant le getter
            <a href={{ url('recipe.show', {id: recipe.getId, slug: recipe.getSlug}) }}>{{ recipe.getTitle() }}</a> #}
            {# sans appeler le getter (le système cherche le getter)#}
            <a href={{ url('recipe.show', {id: recipe.id, slug: recipe.slug}) }}>{{ recipe.title() }}</a>
        </li>
        {% endfor %}
    </ul>

    Voici les recettes disponibles :
    <ul>
        <li>
            <a href={{ url('recipe.show', {id:32, slug:'pate-bol'}) }}>Pâtes bolo (url complète)</a>
        </li>
        <li>
            <a href={{ path('recipe.show', {id:32, slug:'pate-bol'}) }}>Pâtes bolo (url avec seulement le chemin)</a>
        </li>
    </ul>

    Voici le dump de app.current_route directement dans Twig : {{ dump(app.current_route) }} </li>
{% endblock %}